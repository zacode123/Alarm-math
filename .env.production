# Database Configuration
DATABASE_URL=${DATABASE_URL}
PGUSER=${PGUSER}
PGHOST=${PGHOST}
PGPASSWORD=${PGPASSWORD}
PGDATABASE=${PGDATABASE}
PGPORT=${PGPORT}

# Application Configuration
NODE_ENV=production
PORT=5000

# Security Settings (add your actual secrets in production)
SESSION_SECRET=${SESSION_SECRET}

# Cache Settings
CACHE_TTL=3600